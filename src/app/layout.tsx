import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'GoGetWell.ai - Your AI-Powered Health Companion',
  description: 'Experience personalized healthcare guidance powered by advanced AI technology. Get instant answers to your health questions and make informed decisions about your well-being.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
} 